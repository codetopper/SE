<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.karat">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google MapDisplay Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Staff.SHome.SHomeManageListingDisplay"></activity>


        <!--
             The API key for Google MapDisplay-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Customer.CSuperMap.MapDisplay">

        </activity>

        <activity android:name=".Staff.SProfile.SEditProfileDisplay" />
        <activity android:name=".Customer.CProfile.CEditProfileDisplay" />
        <activity android:name=".Login.RegisterDisplay" />
        <activity android:name=".Login.ResetPwDisplay" />
        <activity android:name=".Customer.CHome.CHomeDisplay" />
        <activity android:name=".Staff.SProfile.SProfileDisplay" />
        <activity android:name=".Staff.SOrder.SOrderDisplay" />
        <activity android:name=".Staff.SHome.SHomeDisplay" />
        <activity android:name=".Customer.Cart.CartDisplay" />
        <activity android:name=".Customer.CProfile.CProfileDisplay" />
        <activity android:name=".Customer.COrder.COrderDisplay" />
        <activity android:name=".Login.LoginDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>